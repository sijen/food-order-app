import React from "react";

function Footer() {
  const footer = {
    height: "100px",
    width: "100%",
    color: "white",
    backgroundColor: "green",
  };
  return <footer style={footer}>this is footer section</footer>;
}

export default Footer;
